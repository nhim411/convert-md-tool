{"chunk_id": "1b0e8d3c", "source_file": "main.py", "chunk_index": 0, "header": "Add app directory to path for imports BEFORE importing local modules", "header_path": "Add app directory to path for imports BEFORE importing local modules", "content": "# Add app directory to path for imports BEFORE importing local modules\napp_dir = os.path.dirname(os.path.abspath(__file__))\nif app_dir not in sys.path:\n    sys.path.insert(0, app_dir)\n\nimport customtkinter as ctk\nfrom typing import Optional, List\n\nfrom locales import LABELS\nfrom converter import MarkdownConverter, ConversionResult, AIOptions as ConverterAIOptions\nfrom config_manager import ConfigManager, AppConfig\nfrom components import (\n    FileSelector,\n    FolderOptions,\n    OutputOptions,\n    FormatFilter,\n    ProgressPanel,\n    FormatFilter,\n    ProgressPanel,\n    AIOptions,\n    FilePreview,\n    CollapsibleFrame,\n)\n\nclass MarkdownConverterApp(ctk.CTk):\n    \"\"\"Main application window.\"\"\"\n\n    def __init__(self):\n        super().__init__()", "level": 1, "token_count": 188, "char_count": 753, "prev_header": "", "next_header": "Initialize config manager and load settings", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "5064e646", "source_file": "main.py", "chunk_index": 1, "header": "Initialize config manager and load settings", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings", "content": "# Initialize config manager and load settings\n        self._config_manager = ConfigManager()\n        self._config = self._config_manager.load()", "level": 1, "token_count": 35, "char_count": 143, "prev_header": "Add app directory to path for imports BEFORE importing local modules", "next_header": "Configure window", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "b4b7d39a", "source_file": "main.py", "chunk_index": 2, "header": "Configure window", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window", "content": "# Configure window\n        self.title(LABELS['app_title'])\n        self.geometry(\"1100x700\")\n        self.minsize(900, 600)", "level": 1, "token_count": 30, "char_count": 123, "prev_header": "Initialize config manager and load settings", "next_header": "Set appearance from config", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "48cf92af", "source_file": "main.py", "chunk_index": 3, "header": "Set appearance from config", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config", "content": "# Set appearance from config\n        ctk.set_appearance_mode(self._config.theme)\n        ctk.set_default_color_theme(\"blue\")", "level": 1, "token_count": 31, "char_count": 124, "prev_header": "Configure window", "next_header": "Initialize converter", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "f1baf479", "source_file": "main.py", "chunk_index": 4, "header": "Initialize converter", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter", "content": "# Initialize converter\n        self._converter = MarkdownConverter()\n        self._is_converting = False\n        self._conversion_thread: Optional[threading.Thread] = None", "level": 1, "token_count": 42, "char_count": 171, "prev_header": "Set appearance from config", "next_header": "Files for file mode", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "7ca8d474", "source_file": "main.py", "chunk_index": 5, "header": "Files for file mode", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode", "content": "# Files for file mode\n        self._selected_files: List[str] = []", "level": 1, "token_count": 16, "char_count": 66, "prev_header": "Initialize converter", "next_header": "Create UI", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "db7fe566", "source_file": "main.py", "chunk_index": 6, "header": "Create UI", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI", "content": "# Create UI\n        self._create_widgets()\n        self._bind_events()\n        self._load_config_to_ui()\n\n    def _create_widgets(self):\n        \"\"\"Create and layout all widgets.\"\"\"", "level": 1, "token_count": 45, "char_count": 181, "prev_header": "Files for file mode", "next_header": "Main container", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "497b6283", "source_file": "main.py", "chunk_index": 7, "header": "Main container", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container", "content": "# Main container\n        main_frame = ctk.CTkFrame(self, fg_color=\"transparent\")\n        main_frame.pack(fill=\"both\", expand=True, padx=10, pady=10)", "level": 1, "token_count": 37, "char_count": 148, "prev_header": "Create UI", "next_header": "1. Header Area (Full Width)", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "9c33d1de", "source_file": "main.py", "chunk_index": 8, "header": "1. Header Area (Full Width)", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width)", "content": "# 1. Header Area (Full Width)\n        header_frame = ctk.CTkFrame(main_frame, fg_color=\"transparent\")\n        header_frame.pack(fill=\"x\", pady=(0, 10))\n\n        title_label = ctk.CTkLabel(\n            header_frame,\n            text=f\"🔄 {LABELS['app_title']}\",\n            font=ctk.CTkFont(size=20, weight=\"bold\")\n        )\n        title_label.pack(side=\"left\")", "level": 1, "token_count": 90, "char_count": 360, "prev_header": "Main container", "next_header": "Theme toggle", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "5a85c669", "source_file": "main.py", "chunk_index": 9, "header": "Theme toggle", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle", "content": "# Theme toggle\n        theme_icon = \"☀️\" if self._config.theme == \"dark\" else \"🌙\"\n        self._theme_btn = ctk.CTkButton(\n            header_frame,\n            text=theme_icon,\n            width=32,\n            height=32,\n            command=self._toggle_theme,\n            fg_color=\"transparent\",\n            border_width=1,\n            hover_color=(\"gray85\", \"gray25\")\n        )\n        self._theme_btn.pack(side=\"right\")", "level": 1, "token_count": 106, "char_count": 424, "prev_header": "1. Header Area (Full Width)", "next_header": "2. Content Area (Split Columns)", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "6a318986", "source_file": "main.py", "chunk_index": 10, "header": "2. Content Area (Split Columns)", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns)", "content": "# 2. Content Area (Split Columns)\n        content_frame = ctk.CTkFrame(main_frame, fg_color=\"transparent\")\n        content_frame.pack(fill=\"both\", expand=True)", "level": 1, "token_count": 39, "char_count": 159, "prev_header": "Theme toggle", "next_header": "--- LEFT COLUMN (Inputs & Config) ---", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "cbfccafc", "source_file": "main.py", "chunk_index": 11, "header": "--- LEFT COLUMN (Inputs & Config) ---", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) ---", "content": "# --- LEFT COLUMN (Inputs & Config) ---", "level": 1, "token_count": 9, "char_count": 39, "prev_header": "2. Content Area (Split Columns)", "next_header": "65% width normally, but with pack side=left expand=True it shares space.", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "95f1a581", "source_file": "main.py", "chunk_index": 12, "header": "65% width normally, but with pack side=left expand=True it shares space.", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space.", "content": "# 65% width normally, but with pack side=left expand=True it shares space.", "level": 1, "token_count": 18, "char_count": 74, "prev_header": "--- LEFT COLUMN (Inputs & Config) ---", "next_header": "We can use grid or pack. Pack is easier if we want flexible split.", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "205a69f1", "source_file": "main.py", "chunk_index": 13, "header": "We can use grid or pack. Pack is easier if we want flexible split.", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split.", "content": "# We can use grid or pack. Pack is easier if we want flexible split.\n        left_col = ctk.CTkFrame(content_frame, fg_color=\"transparent\")\n        left_col.pack(side=\"left\", fill=\"both\", expand=True, padx=(0, 10))", "level": 1, "token_count": 53, "char_count": 214, "prev_header": "65% width normally, but with pack side=left expand=True it shares space.", "next_header": "Scrollable container for Left Column if height is small", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "b42cdf03", "source_file": "main.py", "chunk_index": 14, "header": "Scrollable container for Left Column if height is small", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small", "content": "# Scrollable container for Left Column if height is small\n        self._scroll_frame = ctk.CTkScrollableFrame(left_col, fg_color=\"transparent\")\n        self._scroll_frame.pack(fill=\"both\", expand=True, pady=(0, 10))", "level": 1, "token_count": 53, "char_count": 215, "prev_header": "We can use grid or pack. Pack is easier if we want flexible split.", "next_header": "Hero Section (File Selector)", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "3ba95473", "source_file": "main.py", "chunk_index": 15, "header": "Hero Section (File Selector)", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector)", "content": "# Hero Section (File Selector)\n        hero_frame = ctk.CTkFrame(self._scroll_frame, fg_color=(\"gray95\", \"gray15\"), corner_radius=10)\n        hero_frame.pack(fill=\"x\", pady=(0, 10), ipady=5)\n\n        self._file_selector = FileSelector(\n            hero_frame,\n            on_selection_change=self._on_source_change\n        )\n        self._file_selector.pack(fill=\"x\", padx=10, pady=5)", "level": 1, "token_count": 96, "char_count": 384, "prev_header": "Scrollable container for Left Column if height is small", "next_header": "File Preview (Inside Hero)", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "2aaad031", "source_file": "main.py", "chunk_index": 16, "header": "File Preview (Inside Hero)", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero)", "content": "# File Preview (Inside Hero)\n        self._file_preview = FilePreview(\n            hero_frame,\n            on_change=self._on_file_preview_change\n        )", "level": 1, "token_count": 38, "char_count": 155, "prev_header": "Hero Section (File Selector)", "next_header": "Initially hidden", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "65c5d3c2", "source_file": "main.py", "chunk_index": 17, "header": "Initially hidden", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden", "content": "# Initially hidden", "level": 1, "token_count": 4, "char_count": 18, "prev_header": "File Preview (Inside Hero)", "next_header": "Settings Tabs", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "a8fc0078", "source_file": "main.py", "chunk_index": 18, "header": "Settings Tabs", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs", "content": "# Settings Tabs\n        self._tab_view = ctk.CTkTabview(self._scroll_frame, height=350)\n        self._tab_view.pack(fill=\"x\", expand=True)", "level": 1, "token_count": 34, "char_count": 138, "prev_header": "Initially hidden", "next_header": "Create Tabs", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "bf10ee03", "source_file": "main.py", "chunk_index": 19, "header": "Create Tabs", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs", "content": "# Create Tabs\n        tab_general = self._tab_view.add(LABELS.get('tab_general', \"Cấu hình\"))\n        tab_formats = self._tab_view.add(LABELS.get('tab_formats', \"Định dạng\"))\n        tab_advanced = self._tab_view.add(LABELS.get('tab_advanced', \"Nâng cao & AI\"))", "level": 1, "token_count": 65, "char_count": 261, "prev_header": "Settings Tabs", "next_header": "--- Tab 1: General ---", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "348506a4", "source_file": "main.py", "chunk_index": 20, "header": "--- Tab 1: General ---", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General ---", "content": "# --- Tab 1: General ---\n        self._output_options = OutputOptions(tab_general, fg_color=\"transparent\")\n        self._output_options.pack(fill=\"x\", pady=5)\n\n        self._folder_options = FolderOptions(\n            tab_general,\n            fg_color=\"transparent\",\n            on_change=self._on_folder_options_change\n        )\n        self._folder_options.pack(fill=\"x\", pady=5)", "level": 1, "token_count": 95, "char_count": 381, "prev_header": "Create Tabs", "next_header": "--- Tab 2: Formats ---", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "40a1fe3a", "source_file": "main.py", "chunk_index": 21, "header": "--- Tab 2: Formats ---", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats ---", "content": "# --- Tab 2: Formats ---\n        format_note = ctk.CTkLabel(\n            tab_formats,\n            text=\"i️ Chọn các định dạng tệp sẽ được chuyển đổi sang Markdown\",\n            font=ctk.CTkFont(size=11),\n            text_color=\"gray\"\n        )\n        format_note.pack(anchor=\"w\", padx=10, pady=(10, 5))\n\n        self._format_filter = FormatFilter(\n            tab_formats,\n            on_change=self._on_format_change\n        )\n        self._format_filter.pack(fill=\"x\", padx=5)", "level": 1, "token_count": 119, "char_count": 479, "prev_header": "--- Tab 1: General ---", "next_header": "--- Tab 3: Advanced ---", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "c3ddd99a", "source_file": "main.py", "chunk_index": 22, "header": "--- Tab 3: Advanced ---", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced ---", "content": "# --- Tab 3: Advanced ---\n        self._ai_options = AIOptions(tab_advanced, fg_color=\"transparent\")\n        self._ai_options.pack(fill=\"both\", expand=True, padx=5, pady=5)", "level": 1, "token_count": 43, "char_count": 172, "prev_header": "--- Tab 2: Formats ---", "next_header": "Action Button (Bottom of Left Column)", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "44b4ba59", "source_file": "main.py", "chunk_index": 23, "header": "Action Button (Bottom of Left Column)", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column)", "content": "# Action Button (Bottom of Left Column)\n        self._convert_btn = ctk.CTkButton(\n            left_col,\n            text=f\"🚀 {LABELS['start_convert']}\",\n            command=self._start_conversion,\n            height=50,\n            font=ctk.CTkFont(size=16, weight=\"bold\"),\n            fg_color=(\"blue\", \"#1f538d\"),\n        )\n        self._convert_btn.pack(fill=\"x\", side=\"bottom\", pady=(10, 0))", "level": 1, "token_count": 99, "char_count": 396, "prev_header": "--- Tab 3: Advanced ---", "next_header": "--- RIGHT COLUMN (Progress & Logs) ---", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "34e6d420", "source_file": "main.py", "chunk_index": 24, "header": "--- RIGHT COLUMN (Progress & Logs) ---", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) ---", "content": "# --- RIGHT COLUMN (Progress & Logs) ---\n        right_col = ctk.CTkFrame(content_frame, fg_color=(\"gray90\", \"gray13\"), corner_radius=10)\n        right_col.pack(side=\"right\", fill=\"both\", expand=True, ipadx=5, ipady=5)", "level": 1, "token_count": 54, "char_count": 218, "prev_header": "Action Button (Bottom of Left Column)", "next_header": "We pass height=0 or something to imply full fill, but ProgressPanel logic needs update", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "032d2a0b", "source_file": "main.py", "chunk_index": 25, "header": "We pass height=0 or something to imply full fill, but ProgressPanel logic needs update", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update", "content": "# We pass height=0 or something to imply full fill, but ProgressPanel logic needs update", "level": 1, "token_count": 22, "char_count": 88, "prev_header": "--- RIGHT COLUMN (Progress & Logs) ---", "next_header": "to expand its internal scroll frame.", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "ede14421", "source_file": "main.py", "chunk_index": 26, "header": "to expand its internal scroll frame.", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame.", "content": "# to expand its internal scroll frame.\n        self._progress_panel = ProgressPanel(right_col)\n        self._progress_panel.pack(fill=\"both\", expand=True, padx=5, pady=5)\n\n    def _bind_events(self):\n        \"\"\"Bind event handlers.\"\"\"\n        self.protocol(\"WM_DELETE_WINDOW\", self._on_close)\n\n    def _load_config_to_ui(self):\n        \"\"\"Load saved config to UI components.\"\"\"", "level": 1, "token_count": 94, "char_count": 377, "prev_header": "We pass height=0 or something to imply full fill, but ProgressPanel logic needs update", "next_header": "Folder options", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "88d25ff3", "source_file": "main.py", "chunk_index": 27, "header": "Folder options", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options", "content": "# Folder options\n        if self._config.include_subfolders:\n            self._folder_options._recursive_var.set(True)", "level": 1, "token_count": 29, "char_count": 118, "prev_header": "to expand its internal scroll frame.", "next_header": "Output options", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "e11779ab", "source_file": "main.py", "chunk_index": 28, "header": "Output options", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options", "content": "# Output options\n        self._output_options.set_overwrite_existing(self._config.overwrite_existing)", "level": 1, "token_count": 25, "char_count": 101, "prev_header": "Folder options", "next_header": "AI options", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "9b32a704", "source_file": "main.py", "chunk_index": 29, "header": "AI options", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options", "content": "# AI options\n        self._ai_options.load_config({\n            \"extract_images\": self._config.extract_images,\n            \"chunk_enabled\": self._config.chunk_enabled,\n            \"excel_clean_enabled\": self._config.excel_clean_enabled,\n            \"summary_enabled\": self._config.summary_enabled,\n            \"base_url\": self._config.base_url,\n            \"api_key\": self._config.api_key,\n            \"model\": self._config.model,\n        })\n\n    def _save_config(self):\n        \"\"\"Save current UI state to config.\"\"\"", "level": 1, "token_count": 129, "char_count": 517, "prev_header": "Output options", "next_header": "Theme", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "5a28e13f", "source_file": "main.py", "chunk_index": 30, "header": "Theme", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme", "content": "# Theme\n        self._config.theme = ctk.get_appearance_mode().lower()", "level": 1, "token_count": 17, "char_count": 70, "prev_header": "AI options", "next_header": "Folder options", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "e3b3dff4", "source_file": "main.py", "chunk_index": 31, "header": "Folder options", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options", "content": "# Folder options\n        self._config.include_subfolders = self._folder_options.is_recursive", "level": 1, "token_count": 23, "char_count": 92, "prev_header": "Theme", "next_header": "Output options", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "57e7c471", "source_file": "main.py", "chunk_index": 32, "header": "Output options", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options", "content": "# Output options\n        self._config.overwrite_existing = self._output_options.overwrite_existing", "level": 1, "token_count": 24, "char_count": 98, "prev_header": "Folder options", "next_header": "AI options", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "5c0ae518", "source_file": "main.py", "chunk_index": 33, "header": "AI options", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options", "content": "# AI options\n        ai_config = self._ai_options.get_config()\n        self._config.extract_images = ai_config.get(\"extract_images\", False)\n        self._config.chunk_enabled = ai_config.get(\"chunk_enabled\", False)\n        self._config.excel_clean_enabled = ai_config.get(\"excel_clean_enabled\", False)\n        self._config.summary_enabled = ai_config.get(\"summary_enabled\", False)\n\n        self._config.base_url = ai_config.get(\"base_url\", \"https://api.openai.com/v1\")\n        self._config.api_key = ai_config.get(\"api_key\", \"\")\n        self._config.model = ai_config.get(\"model\", \"gpt-4o-mini\")\n\n        self._config_manager.save(self._config)\n\n    def _on_source_change(self, path: str, mode: str):\n        \"\"\"Handle source selection change.\"\"\"", "level": 1, "token_count": 186, "char_count": 746, "prev_header": "Output options", "next_header": "Enable/disable folder options based on mode", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "38045cf4", "source_file": "main.py", "chunk_index": 34, "header": "Enable/disable folder options based on mode", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode", "content": "# Enable/disable folder options based on mode\n        self._folder_options.set_enabled(mode == \"folder\")\n\n        if path:\n            if mode == \"file\":\n                if os.path.isfile(path):", "level": 1, "token_count": 48, "char_count": 194, "prev_header": "AI options", "next_header": "Single file - show preview", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "e8016cc8", "source_file": "main.py", "chunk_index": 35, "header": "Single file - show preview", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview", "content": "# Single file - show preview\n                    self._selected_files = [path]\n                    self._file_preview.set_files(self._selected_files)\n                    self._file_preview.pack(fill=\"x\", padx=15, pady=(0, 10), after=self._file_selector)\n            else:", "level": 1, "token_count": 67, "char_count": 271, "prev_header": "Enable/disable folder options based on mode", "next_header": "Folder mode - scan and show preview", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "606cbab5", "source_file": "main.py", "chunk_index": 36, "header": "Folder mode - scan and show preview", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview", "content": "# Folder mode - scan and show preview\n                self.after(100, self._scan_folder)  # Small delay to ensure UI ready\n                self._file_preview.pack(fill=\"x\", padx=15, pady=(0, 10), after=self._file_selector)\n        else:", "level": 1, "token_count": 59, "char_count": 236, "prev_header": "Single file - show preview", "next_header": "No selection - hide preview", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "d7824182", "source_file": "main.py", "chunk_index": 37, "header": "No selection - hide preview", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview", "content": "# No selection - hide preview\n            self._file_preview.pack_forget()\n            self._selected_files = []\n\n    def _on_format_change(self):\n        \"\"\"Handle format selection change.\"\"\"\n        if self._file_selector.is_folder_mode() and self._file_selector.selected_path:\n            self._scan_folder()\n\n    def _on_folder_options_change(self):\n        \"\"\"Handle folder options change.\"\"\"\n        if self._file_selector.is_folder_mode() and self._file_selector.selected_path:\n            self._scan_folder()\n\n    def _scan_folder(self):\n        \"\"\"Scan folder for files and update preview.\"\"\"\n        path = self._file_selector.selected_path\n        if not path or not os.path.isdir(path):\n            return", "level": 1, "token_count": 179, "char_count": 717, "prev_header": "Folder mode - scan and show preview", "next_header": "Show loading state (optional, can be improved)", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "cad98904", "source_file": "main.py", "chunk_index": 38, "header": "Show loading state (optional, can be improved)", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved)", "content": "# Show loading state (optional, can be improved)\n        self._progress_panel.set_status(\"Đang quét thư mục...\")", "level": 1, "token_count": 28, "char_count": 112, "prev_header": "No selection - hide preview", "next_header": "Get settings", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "4f6767f4", "source_file": "main.py", "chunk_index": 39, "header": "Get settings", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings", "content": "# Get settings\n        selected_formats = self._format_filter.get_selected_formats()\n        recursive = self._folder_options.is_recursive\n        max_depth = self._folder_options.max_depth", "level": 1, "token_count": 47, "char_count": 189, "prev_header": "Show loading state (optional, can be improved)", "next_header": "Run scan in background to avoid freezing UI", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "87dd9709", "source_file": "main.py", "chunk_index": 40, "header": "Run scan in background to avoid freezing UI", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI", "content": "# Run scan in background to avoid freezing UI\n        def scan_task():\n            files = self._converter.scan_folder(\n                folder_path=path,\n                recursive=recursive,\n                max_depth=max_depth,\n                allowed_extensions=self._converter.get_extensions_for_formats(selected_formats)\n            )", "level": 1, "token_count": 84, "char_count": 337, "prev_header": "Get settings", "next_header": "Update UI on main thread", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "bc93eb82", "source_file": "main.py", "chunk_index": 41, "header": "Update UI on main thread", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread", "content": "# Update UI on main thread\n            self.after(0, lambda: self._update_preview_after_scan(files))\n\n        threading.Thread(target=scan_task, daemon=True).start()\n\n    def _update_preview_after_scan(self, files: List[str]):\n        \"\"\"Update preview with scanned files.\"\"\"\n        self._selected_files = files\n        self._file_preview.set_files(files)\n        self._progress_panel.set_status(LABELS['ready'])\n\n        if not files:\n            self._progress_panel.log_message(LABELS['error_empty_folder'], \"info\")\n\n    def _on_file_preview_change(self):\n        \"\"\"Handle file preview selection change.\"\"\"\n        self._selected_files = self._file_preview.get_selected_files()\n\n    def _toggle_theme(self):\n        \"\"\"Toggle between light and dark theme.\"\"\"\n        current = ctk.get_appearance_mode()\n        if current == \"Dark\":\n            ctk.set_appearance_mode(\"light\")\n            self._theme_btn.configure(text=\"🌙\")\n        else:\n            ctk.set_appearance_mode(\"dark\")\n            self._theme_btn.configure(text=\"☀️\")\n\n    def _validate_inputs(self) -> bool:\n        \"\"\"Validate user inputs before conversion.\"\"\"", "level": 1, "token_count": 283, "char_count": 1132, "prev_header": "Run scan in background to avoid freezing UI", "next_header": "Check source path", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "5bbfd6c4", "source_file": "main.py", "chunk_index": 42, "header": "Check source path", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path", "content": "# Check source path\n        if not self._file_selector.selected_path:\n            self._show_error(LABELS['error_no_source'])\n            return False", "level": 1, "token_count": 37, "char_count": 150, "prev_header": "Update UI on main thread", "next_header": "Check path exists", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "02c51357", "source_file": "main.py", "chunk_index": 43, "header": "Check path exists", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists", "content": "# Check path exists\n        if not os.path.exists(self._file_selector.selected_path):\n            self._show_error(LABELS['error_invalid_path'])\n            return False", "level": 1, "token_count": 42, "char_count": 169, "prev_header": "Check source path", "next_header": "Check file preview selection", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "6c528a6a", "source_file": "main.py", "chunk_index": 44, "header": "Check file preview selection", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection", "content": "# Check file preview selection\n        if self._file_selector.is_folder_mode():\n            if not self._format_filter.has_selection():\n                self._show_error(LABELS['error_no_formats'])\n                return False\n        else:\n            if self._file_preview.has_files() and not self._file_preview.has_selected():\n                self._show_error(\"Vui lòng chọn ít nhất một tệp để chuyển đổi\")\n                return False\n\n        return True\n\n    def _show_error(self, message: str):\n        \"\"\"Show error in progress panel.\"\"\"\n        self._progress_panel.log_message(message, \"error\")\n\n    def _start_conversion(self):\n        \"\"\"Start the conversion process.\"\"\"\n        if self._is_converting:", "level": 1, "token_count": 178, "char_count": 713, "prev_header": "Check path exists", "next_header": "Stop conversion", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "3a43d723", "source_file": "main.py", "chunk_index": 45, "header": "Stop conversion", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection > Stop conversion", "content": "# Stop conversion\n            self._converter.request_stop()\n            self._convert_btn.configure(\n                text=f\"🚀 {LABELS['start_convert']}\",\n                state=\"disabled\"\n            )\n            return\n\n        if not self._validate_inputs():\n            return", "level": 1, "token_count": 70, "char_count": 280, "prev_header": "Check file preview selection", "next_header": "Update UI state", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "121ec3f5", "source_file": "main.py", "chunk_index": 46, "header": "Update UI state", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection > Stop conversion > Update UI state", "content": "# Update UI state\n        self._is_converting = True\n        self._convert_btn.configure(text=f\"⏹ {LABELS['stop_convert']}\")\n        self._progress_panel.reset()\n        self._progress_panel.set_status(LABELS['processing'])", "level": 1, "token_count": 55, "char_count": 223, "prev_header": "Stop conversion", "next_header": "Apply AI options to converter", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "f86f71fd", "source_file": "main.py", "chunk_index": 47, "header": "Apply AI options to converter", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection > Stop conversion > Update UI state > Apply AI options to converter", "content": "# Apply AI options to converter\n        ai_cfg = self._ai_options.get_config()\n        ai_opts = ConverterAIOptions(\n            extract_images=ai_cfg.get(\"extract_images\", False),\n            chunk_enabled=ai_cfg.get(\"chunk_enabled\", False),\n            excel_clean_enabled=ai_cfg.get(\"excel_clean_enabled\", False),\n            summary_enabled=ai_cfg.get(\"summary_enabled\", False),\n            base_url=ai_cfg.get(\"base_url\", \"https://api.openai.com/v1\"),\n            api_key=ai_cfg.get(\"api_key\", \"\"),\n            model=ai_cfg.get(\"model\", \"gpt-4o-mini\"),\n        )\n        self._converter.set_ai_options(ai_opts)", "level": 1, "token_count": 153, "char_count": 615, "prev_header": "Update UI state", "next_header": "Start conversion in background thread", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "a5df6461", "source_file": "main.py", "chunk_index": 48, "header": "Start conversion in background thread", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection > Stop conversion > Update UI state > Apply AI options to converter > Start conversion in background thread", "content": "# Start conversion in background thread\n        self._conversion_thread = threading.Thread(\n            target=self._run_conversion,\n            daemon=True\n        )\n        self._conversion_thread.start()\n\n    def _run_conversion(self):\n        \"\"\"Run conversion in background thread.\"\"\"\n        try:\n            source_path = self._file_selector.selected_path\n            output_dir = self._output_options.output_path\n\n            if self._file_selector.is_folder_mode():", "level": 1, "token_count": 118, "char_count": 474, "prev_header": "Apply AI options to converter", "next_header": "Folder conversion - Use files from preview!", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "a9ad5b36", "source_file": "main.py", "chunk_index": 49, "header": "Folder conversion - Use files from preview!", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection > Stop conversion > Update UI state > Apply AI options to converter > Start conversion in background thread > Folder conversion - Use files from preview!", "content": "# Folder conversion - Use files from preview!", "level": 1, "token_count": 11, "char_count": 45, "prev_header": "Start conversion in background thread", "next_header": "We already scanned, so we trust the preview list.", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "308bacdf", "source_file": "main.py", "chunk_index": 50, "header": "We already scanned, so we trust the preview list.", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection > Stop conversion > Update UI state > Apply AI options to converter > Start conversion in background thread > Folder conversion - Use files from preview! > We already scanned, so we trust the preview list.", "content": "# We already scanned, so we trust the preview list.\n                files_to_convert = self._file_preview.get_selected_files()", "level": 1, "token_count": 31, "char_count": 126, "prev_header": "Folder conversion - Use files from preview!", "next_header": "Check directly, if list empty logic below handles it", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "4459ba15", "source_file": "main.py", "chunk_index": 51, "header": "Check directly, if list empty logic below handles it", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection > Stop conversion > Update UI state > Apply AI options to converter > Start conversion in background thread > Folder conversion - Use files from preview! > We already scanned, so we trust the preview list. > Check directly, if list empty logic below handles it", "content": "# Check directly, if list empty logic below handles it\n            else:", "level": 1, "token_count": 18, "char_count": 72, "prev_header": "We already scanned, so we trust the preview list.", "next_header": "File mode - convert selected files", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "f28a2a2d", "source_file": "main.py", "chunk_index": 52, "header": "File mode - convert selected files", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection > Stop conversion > Update UI state > Apply AI options to converter > Start conversion in background thread > Folder conversion - Use files from preview! > We already scanned, so we trust the preview list. > Check directly, if list empty logic below handles it > File mode - convert selected files", "content": "# File mode - convert selected files\n                files_to_convert = self._file_preview.get_selected_files() if self._file_preview.has_files() else [source_path]\n\n            if not files_to_convert:\n                 self.after(0, lambda: self._progress_panel.log_message(\n                    LABELS['error_empty_folder'], \"info\"\n                ))\n                 return\n\n            total = len(files_to_convert)\n            overwrite = self._output_options.overwrite_existing\n\n            self.after(0, lambda: self._progress_panel.set_progress(0, total))\n\n            success_count = 0\n            for i, file_path in enumerate(files_to_convert):\n                if self._converter._stop_requested:\n                    break\n\n                result = self._converter.convert_file(file_path, output_dir, overwrite=overwrite)\n                if result.success:\n                    success_count += 1\n\n                self.after(0, lambda r=result, c=i+1, t=total: self._on_progress(c, t, r))\n\n            self.after(0, lambda: self._progress_panel.show_done(success_count, total))\n\n        except Exception as e:\n            self.after(0, lambda: self._progress_panel.log_message(\n                str(e), \"error\"\n            ))\n\n        finally:", "level": 1, "token_count": 312, "char_count": 1251, "prev_header": "Check directly, if list empty logic below handles it", "next_header": "Reset UI state", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "ecc89f65", "source_file": "main.py", "chunk_index": 53, "header": "Reset UI state", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection > Stop conversion > Update UI state > Apply AI options to converter > Start conversion in background thread > Folder conversion - Use files from preview! > We already scanned, so we trust the preview list. > Check directly, if list empty logic below handles it > File mode - convert selected files > Reset UI state", "content": "# Reset UI state\n            self.after(0, self._conversion_complete)\n\n    def _on_progress(self, current: int, total: int, result: ConversionResult):\n        \"\"\"Callback for progress updates from converter.\"\"\"\n        def update():\n            self._progress_panel.set_progress(current, total)\n            self._progress_panel.set_status(\n                LABELS['converting'].format(current=current, total=total)\n            )\n            self._progress_panel.log_conversion_result(\n                source=result.source_path,\n                output=result.output_path,\n                success=result.success,\n                error=result.error_message,\n                skipped=result.skipped,\n                images_extracted=result.images_extracted,\n                images_described=result.images_described\n            )\n\n        self.after(0, update)\n\n    def _conversion_complete(self):\n        \"\"\"Reset UI after conversion completes.\"\"\"\n        self._is_converting = False\n        self._convert_btn.configure(\n            text=f\"🚀 {LABELS['start_convert']}\",\n            state=\"normal\"\n        )\n        self._converter.reset_stop()\n\n    def _on_close(self):\n        \"\"\"Handle window close.\"\"\"", "level": 1, "token_count": 299, "char_count": 1198, "prev_header": "File mode - convert selected files", "next_header": "Save config before closing", "tags": ["directory", "path", "imports", "importing", "local"]}
{"chunk_id": "89ccf60a", "source_file": "main.py", "chunk_index": 54, "header": "Save config before closing", "header_path": "Add app directory to path for imports BEFORE importing local modules > Initialize config manager and load settings > Configure window > Set appearance from config > Initialize converter > Files for file mode > Create UI > Main container > 1. Header Area (Full Width) > Theme toggle > 2. Content Area (Split Columns) > --- LEFT COLUMN (Inputs & Config) --- > 65% width normally, but with pack side=left expand=True it shares space. > We can use grid or pack. Pack is easier if we want flexible split. > Scrollable container for Left Column if height is small > Hero Section (File Selector) > File Preview (Inside Hero) > Initially hidden > Settings Tabs > Create Tabs > --- Tab 1: General --- > --- Tab 2: Formats --- > --- Tab 3: Advanced --- > Action Button (Bottom of Left Column) > --- RIGHT COLUMN (Progress & Logs) --- > We pass height=0 or something to imply full fill, but ProgressPanel logic needs update > to expand its internal scroll frame. > Folder options > Output options > AI options > Theme > Folder options > Output options > AI options > Enable/disable folder options based on mode > Single file - show preview > Folder mode - scan and show preview > No selection - hide preview > Show loading state (optional, can be improved) > Get settings > Run scan in background to avoid freezing UI > Update UI on main thread > Check source path > Check path exists > Check file preview selection > Stop conversion > Update UI state > Apply AI options to converter > Start conversion in background thread > Folder conversion - Use files from preview! > We already scanned, so we trust the preview list. > Check directly, if list empty logic below handles it > File mode - convert selected files > Reset UI state > Save config before closing", "content": "# Save config before closing\n        self._save_config()\n\n        if self._is_converting:\n            self._converter.request_stop()\n        self.destroy()\n\ndef main():\n    \"\"\"Application entry point.\"\"\"\n    app = MarkdownConverterApp()\n    app.mainloop()\n\nif __name__ == \"__main__\":\n    main()", "level": 1, "token_count": 73, "char_count": 294, "prev_header": "Reset UI state", "next_header": "", "tags": ["directory", "path", "imports", "importing", "local"]}
