# Core dependencies (optimized for minimal build size)
markitdown[pdf,docx,pptx,xlsx]>=0.1.0
customtkinter>=5.2.0
Pillow>=10.0.0

# Image extraction from PDF
PyMuPDF>=1.23.0

# AI Vision (optional - for image description)
openai>=1.0.0
google-generativeai>=0.3.0
